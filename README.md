# Random Generator Comparison Tool
This java file compares data generated from two different generation methods, available in three sample sizes (10, 1000, 100000).
It provides a side-by-side comparison of how java.util.Random and java.lang.Math.random() perform across various sample sizes, providing mean, std, min, and max values. 
